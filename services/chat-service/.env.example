PORT=5003
DB_HOST=localhost
DB_PORT=5432
DB_NAME=legalai_db
DB_USER=postgres
DB_PASSWORD=10092002
JWT_SECRET=your-secret-key-change-this

# RAG: embed câu hỏi + search văn bản (document-service) + trả lời bằng LLM
DOCUMENT_SERVICE_URL=http://localhost:5002

# Auth service để validate token (khuyên dùng)
AUTH_SERVICE_URL=http://localhost:5001

# Ưu tiên Gemini cho trả lời (chỉ sinh văn bản — KHÔNG dùng làm vector embedding)
# GEMINI_API_KEY=AIza...
# GEMINI_MODEL=gemini-1.5-pro
#
# Tra cứu AI / RAG: embedding do document-service xử lý bằng OpenAI 1536d.
# Xem services/document-service/.env.example.

# Hoặc dùng OpenAI (fallback nếu không có Gemini)
# OPENAI_API_KEY=sk-...
# OPENAI_CHAT_MODEL=gpt-4o-mini
